export interface PhotoData {
    camera: string
    fNumber: string
    focalLength: string
    full: string
    iso: string
    lens: string
    raw: string
    shutterSpeed: string
    uploadTimestamp: Date
    id: string
  }